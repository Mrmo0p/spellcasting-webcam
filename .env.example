# Public URL of the separately deployed spellbound-pvp Worker.
# Local development defaults to http://localhost:8787 when this is omitted.
NEXT_PUBLIC_PVP_API_URL=https://spellbound-pvp.thanapoomplachewa.workers.dev
